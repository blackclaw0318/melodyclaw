# MelodyClaw V2.0 - 互动式卡拉 OK 系统需求文档

> 版本：2.0 | 日期：2026-03-19 | 状态：草案

## 📋 文档修订记录

| 版本 | 日期 | 作者 | 变更说明 |
|------|------|------|----------|
| 1.0 | 2026-03-18 | blackclaw0318 | 初始版本 - 歌声克隆系统 |
| 2.0 | 2026-03-19 | blackclaw0318 | 重大升级 - 互动式卡拉 OK 系统 |

---

## 1. 项目概述

### 1.1 产品定位

MelodyClaw V2.0 是一款**互动式卡拉 OK 应用**，类似"全民 K 歌"的 Web 版本。核心创新点：

- 🎤 **摄像头实时拍摄** - 用户可录制自己唱歌的视频
- 🦞 **AR 小龙虾互动** - 虚拟小龙虾可与视频中的人物互动
- 📝 **动态歌词字幕** - 可自由拖动位置的滚动歌词
- 🎵 **智能伴奏播放** - 3-2-1 倒计时后自动播放伴奏

### 1.2 目标用户

- 喜欢唱歌的普通用户
- 想要录制唱歌短视频的用户
- 喜欢创意互动体验的年轻人

### 1.3 核心场景

```
用户打开 App → 选择想唱的歌曲 → 进入拍摄页面
→ 调整小龙虾和歌词位置 → 3-2-1 倒计时 → 开始演唱
→ 演唱过程中与小龙虾互动 → 录制完成 → 预览/保存/分享
```

---

## 2. 功能需求

### 2.1 功能模块总览

| 模块 | 功能 | 优先级 | 复杂度 |
|------|------|--------|--------|
| 首页歌曲列表 | 展示可合唱歌曲 | P0 | 低 |
| 拍摄页面 | 摄像头调用、预览 | P0 | 高 |
| 歌词字幕 | 滚动歌词、位置拖动 | P0 | 中 |
| 小龙虾动画 | AR 互动、位置拖动 | P0 | 高 |
| 录制功能 | 倒计时、音视频录制 | P0 | 高 |
| 视频预览 | 回放、保存、分享 | P1 | 中 |
| 歌曲上传 | 用户上传歌曲 | P1 | 低 |
| 用户系统 | 登录、作品管理 | P2 | 中 |

### 2.2 详细功能描述

#### 2.2.1 首页 - 歌曲列表

**页面样式参考**：全民 K 歌首页卡片式布局

**功能点**：
- 网格展示歌曲封面卡片（2 列或 3 列）
- 每首歌曲显示：封面图、歌名、歌手、时长、已合唱次数
- 搜索框：支持歌名/歌手搜索
- 分类筛选：流行、摇滚、民谣等
- 上传按钮：用户上传新歌（P1）

**数据结构**：
```json
{
  "songs": [
    {
      "id": 1,
      "title": "测试歌曲",
      "artist": "原唱歌手",
      "duration": 245,
      "cover_url": "/covers/song_1.jpg",
      "accompaniment_url": "/audio/song_1伴奏.wav",
      "lyrics": "[00:05.00] 第一句歌词...",
      "duet_count": 128,
      "bpm": 120
    }
  ]
}
```

---

#### 2.2.2 拍摄页面 - 核心功能

**页面布局**（竖屏模式）：

```
┌─────────────────────────────────┐
│  ←返回     拍摄中     [切换镜头] │
├─────────────────────────────────┤
│                                 │
│         ┌─────────────┐         │
│         │             │         │
│         │  摄像头画面  │         │
│         │  (全屏预览)  │         │
│         │             │         │
│         │    🦞        │         │
│         │  (小龙虾)    │         │
│         │             │         │
│         └─────────────┘         │
│                                 │
├─────────────────────────────────┤
│  [歌词] [动画]    [🎤录制]    │
│  按钮   按钮       大按钮       │
└─────────────────────────────────┘
```

**功能点**：

1. **摄像头调用**
   - 默认调用后置摄像头
   - 支持前后摄像头切换
   - 分辨率：720p/1080p 可选
   - 帧率：30fps

2. **歌词字幕模块**
   - 点击"歌词"按钮展开/收起歌词面板
   - 歌词自动滚动（跟随伴奏时间）
   - 当前歌词高亮放大
   - **支持拖动**：用户可长按歌词区域拖动到任意位置
   - 录制开始后位置锁定

3. **小龙虾动画模块**
   - 点击"动画"按钮显示/隐藏小龙虾
   - 小龙虾随音乐节奏摆动（钳子开合、身体晃动）
   - **支持拖动**：用户可长按小龙虾拖动到任意位置
   - 录制开始后位置锁定
   - 可选：小龙虾吐出气泡音符特效

4. **录制功能**
   - 点击录制按钮 → 弹出倒计时设置（默认 3 秒）
   - 3-2-1 倒计时动画（屏幕中央大数字）
   - 倒计时结束 → 自动播放伴奏
   - 开始录制：同时录制摄像头视频 + 麦克风音频 + 伴奏混音
   - 录制中显示：录制时长、暂停按钮、结束按钮
   - 录制时长限制：最长 5 分钟（可配置）

---

#### 2.2.3 录制倒计时设置

**倒计时配置弹窗**：

```
┌─────────────────────────────┐
│  倒计时设置              [×] │
├─────────────────────────────┤
│                             │
│  倒计时时长：[3] 秒          │
│  ▬▬▬▬●▬▬▬▬▬▬               │
│  1    3    5    10          │
│                             │
│  ☑ 倒计时数字跟随 BPM 节奏   │
│                             │
│  节奏预览：[▶ 播放]         │
│                             │
│         [取消] [确定]       │
└─────────────────────────────┘
```

**BPM 同步逻辑**：
- 读取歌曲 BPM 信息
- 计算每拍时长：`beat_duration = 60 / BPM`
- 倒计时数字切换时机与节拍对齐
- 示例：BPM=120 → 每拍 0.5 秒 → "3"持续 2 拍、"2"持续 2 拍、"1"持续 2 拍

---

#### 2.2.4 视频预览与保存

**预览页面**：

```
┌─────────────────────────────────┐
│  ←返回        预览              │
├─────────────────────────────────┤
│                                 │
│         ┌─────────────┐         │
│         │             │         │
│         │  视频播放   │         │
│         │  (含小龙虾)  │         │
│         │             │         │
│         └─────────────┘         │
│                                 │
│  [◀] ━━━━━━━●━━━━━━━ [▶]      │
│  00:00 / 03:45                  │
│                                 │
├─────────────────────────────────┤
│  [🔄 重录]  [💾 保存]  [📤 分享] │
└─────────────────────────────────┘
```

**功能点**：
- 视频播放（含小龙虾动画和歌词字幕）
- 进度条拖动
- 重录：返回拍摄页面重新录制
- 保存：下载视频到本地（MP4 格式）
- 分享：生成分享链接（P2 功能）

---

### 2.3 后端 API 需求变更

#### 2.3.1 新增 API 端点

| 端点 | 方法 | 描述 |
|------|------|------|
| `/api/v2/songs` | GET | 获取歌曲列表（含封面、BPM 等） |
| `/api/v2/songs/{id}/accompaniment` | GET | 获取伴奏音频 |
| `/api/v2/songs/{id}/lyrics` | GET | 获取歌词（含时间戳） |
| `/api/v2/recordings` | POST | 上传录制作品 |
| `/api/v2/recordings/{id}` | GET | 获取作品详情 |
| `/api/v2/recordings/{id}/video` | GET | 下载作品视频 |

#### 2.3.2 数据库模型变更

**新增表：recordings（录制作品）**

```sql
CREATE TABLE recordings (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    uuid VARCHAR(64) UNIQUE NOT NULL,
    song_id INTEGER NOT NULL,
    user_id INTEGER,  -- P2: 用户系统
    video_file VARCHAR(512),
    duration FLOAT,
    lobster_position JSON,  -- 小龙虾位置 {x, y}
    lyrics_position JSON,   -- 歌词位置 {x, y}
    view_count INTEGER DEFAULT 0,
    like_count INTEGER DEFAULT 0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (song_id) REFERENCES songs(id)
);
```

**修改表：songs（歌曲）**

```sql
ALTER TABLE songs ADD COLUMN cover_file VARCHAR(512);
ALTER TABLE songs ADD COLUMN bpm INTEGER;
ALTER TABLE songs ADD COLUMN accompaniment_file VARCHAR(512);
ALTER TABLE songs ADD COLUMN duet_count INTEGER DEFAULT 0;
```

---

## 3. 技术架构

### 3.1 前端技术栈升级

| 技术 | 原方案 | 新方案 | 说明 |
|------|--------|--------|------|
| 摄像头 | - | MediaDevices API | 浏览器原生摄像头访问 |
| 视频录制 | - | MediaRecorder API | 浏览器原生录制 |
| 动画 | CSS 动画 | Canvas + requestAnimationFrame | 高性能动画渲染 |
| 音频播放 | HTML5 Audio | Web Audio API | 精确控制、低延迟 |
| 状态管理 | Pinia | Pinia | 保持不变 |
| UI 框架 | 原生 | TailwindCSS | 快速样式开发 |

### 3.2 核心组件设计

#### 3.2.1 CameraView 组件

```vue
<template>
  <div class="camera-view">
    <video ref="videoElement" autoplay playsinline />
    <canvas ref="canvasElement" class="overlay" />
  </div>
</template>

<script setup>
// 摄像头初始化
// 视频流捕获
// 画布叠加（小龙虾、歌词）
</script>
```

#### 3.2.2 LobsterAnimation 组件

```vue
<template>
  <div class="lobster" :style="positionStyle">
    <img :src="currentFrame" draggable="false" />
  </div>
</template>

<script setup>
// 帧动画播放
// 节奏响应（根据音频频谱）
// 拖拽交互
</script>
```

#### 3.2.3 LyricsScroll 组件

```vue
<template>
  <div class="lyrics-container" :style="positionStyle">
    <div 
      v-for="(line, index) in lyrics" 
      :key="index"
      :class="{ active: currentIndex === index }"
    >
      {{ line.text }}
    </div>
  </div>
</template>

<script setup>
// 歌词滚动
// 当前行高亮
// 拖拽交互
</script>
```

#### 3.2.4 Recorder 组件

```vue
<template>
  <button @click="startRecording">
    {{ isRecording ? '停止' : '录制' }}
  </button>
</template>

<script setup>
// 倒计时逻辑
// MediaRecorder 封装
// 音视频混音
// 文件生成
</script>
```

### 3.3 后端技术栈变更

| 服务 | 原方案 | 新方案 | 说明 |
|------|--------|--------|------|
| Web 框架 | FastAPI | FastAPI | 保持不变 |
| 文件存储 | 本地 | 本地 + 云存储 (可选) | 视频文件较大 |
| 视频处理 | - | FFmpeg | 视频编码、合并 |
| 数据库 | SQLite | SQLite → PostgreSQL | 根据规模选择 |

### 3.4 音视频处理流程

```
┌─────────────────────────────────────────────────────────┐
│                    录制流程                              │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  摄像头 ──→ MediaStream ──┐                            │
│                           ├──→ MediaRecorder ──→ WebM  │
│  麦克风 ──→ MediaStream ──┤                            │
│                           │                            │
│  伴奏 ──→ Web Audio ──────┘                            │
│                                                         │
│  录制完成 ──→ 上传后端 ──→ FFmpeg 转码 ──→ MP4         │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 4. 开发计划

### 4.1 阶段划分

#### 阶段 1：基础框架搭建（预计 3 天）

- [ ] 前端项目重构（Vue3 + Vite + TailwindCSS）
- [ ] 摄像头调用测试
- [ ] MediaRecorder 录制测试
- [ ] 后端 API 结构调整

#### 阶段 2：核心功能开发（预计 5 天）

- [ ] 首页歌曲列表页面
- [ ] 拍摄页面布局
- [ ] 歌词滚动组件
- [ ] 小龙虾动画组件
- [ ] 拖拽交互实现

#### 阶段 3：录制功能（预计 4 天）

- [ ] 倒计时逻辑
- [ ] 音视频同步录制
- [ ] 伴奏混音
- [ ] 视频预览

#### 阶段 4：后端支持（预计 3 天）

- [ ] 作品上传 API
- [ ] 视频存储
- [ ] 数据库模型更新
- [ ] 文件服务优化

#### 阶段 5：优化与测试（预计 3 天）

- [ ] 性能优化（动画帧率、内存）
- [ ] 兼容性测试（不同浏览器）
- [ ] UI/UX 优化
- [ ] Bug 修复

**总预计：18 个工作日**

---

## 5. 技术难点与解决方案

### 5.1 难点 1：音视频同步

**问题**：摄像头视频、麦克风音频、伴奏音乐三者同步

**解决方案**：
- 使用 Web Audio API 统一时钟源
- 伴奏通过 Web Audio 播放（而非 HTML5 Audio）
- MediaRecorder 录制时同步捕获所有音轨
- 录制开始前进行时钟同步校准

### 5.2 难点 2：小龙虾动画性能

**问题**：动画需要流畅（60fps）且响应音乐节奏

**解决方案**：
- 使用 Canvas 而非 DOM 动画
- 预加载动画帧序列
- 使用 Web Audio AnalyserNode 获取频谱数据
- 简化节奏响应逻辑（低频检测 → 钳子开合）

### 5.3 难点 3：拖拽后位置锁定

**问题**：录制前可拖动，录制中位置固定

**解决方案**：
- 状态管理：`isRecording` 标志位
- 拖拽组件监听状态，录制中禁用拖拽
- 录制开始时记录当前位置坐标
- 预览/回放时使用锁定位置

### 5.4 难点 4：视频文件大小

**问题**：录制的视频文件可能很大（5 分钟 1080p ≈ 500MB）

**解决方案**：
- 录制时控制码率（MediaRecorder mimeType + videoBitsPerSecond）
- 后端 FFmpeg 转码压缩（H.264 + AAC）
- 提供分辨率选择（720p/1080p）
- 云存储 + CDN 分发（P2）

---

## 6. 验收标准

### 6.1 功能验收

| 功能 | 验收标准 |
|------|----------|
| 歌曲列表 | 能正确展示歌曲，点击可进入拍摄页 |
| 摄像头 | 前后摄像头可切换，画面清晰 |
| 歌词 | 歌词正确滚动，可拖动位置 |
| 小龙虾 | 动画流畅，可拖动位置 |
| 倒计时 | 倒计时准确，与 BPM 同步 |
| 录制 | 录制视频包含画面、人声、伴奏 |
| 预览 | 可播放录制的视频 |
| 保存 | 可下载 MP4 视频文件 |

### 6.2 性能验收

| 指标 | 目标值 |
|------|--------|
| 页面加载时间 | < 3 秒 |
| 摄像头启动时间 | < 2 秒 |
| 动画帧率 | ≥ 50 fps |
| 录制延迟 | < 100ms |
| 视频保存时间 | < 10 秒（1 分钟视频） |

### 6.3 兼容性验收

| 平台 | 浏览器 | 支持程度 |
|------|--------|----------|
| iOS | Safari | ✅ 完全支持 |
| Android | Chrome | ✅ 完全支持 |
| Desktop | Chrome/Edge | ✅ 完全支持 |
| Desktop | Firefox | ⚠️ 部分支持 |
| Desktop | Safari | ⚠️ 部分支持 |

---

## 7. 风险评估

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| 浏览器兼容性 | 中 | 高 | 优先支持 Chrome/Safari，其他标记为实验性 |
| 性能问题 | 中 | 中 | 早期性能测试，设置降级方案 |
| 视频文件过大 | 高 | 中 | 压缩转码，云存储 |
| 音视频不同步 | 低 | 高 | 充分测试，提供手动校准 |

---

## 8. 附录

### 8.1 参考产品

- 全民 K 歌
- 唱吧
- TikTok 音乐拍摄
- Instagram Reels

### 8.2 技术文档

- [MediaDevices API](https://developer.mozilla.org/en-US/docs/Web/API/MediaDevices)
- [MediaRecorder API](https://developer.mozilla.org/en-US/docs/Web/API/MediaRecorder)
- [Web Audio API](https://developer.mozilla.org/en-US/docs/Web/Web_Audio_API)
- [Canvas API](https://developer.mozilla.org/en-US/docs/Web/API/Canvas_API)

### 8.3 设计资源

- 小龙虾动画素材：待设计
- UI 设计稿：待输出
- 歌曲封面：用户上传或默认图

---

**文档状态**：待评审  
**下一步**：技术评审 → UI 设计 → 开发实施
