# RVC v2 Dependencies
# Install separately due to custom requirements

# Core RVC dependencies
fairseq==0.12.2
librosa>=0.10.0
praat-parselmouth>=0.4.3
pyworld>=0.3.2
faiss-cpu>=1.7.4
torchcrepe>=0.0.20
rmvpe>=0.1.0

# Note: Some RVC components may require compilation
# If installation fails, try:
#   pip install --no-build-isolation fairseq
# Or use pre-built wheels from:
#   https://github.com/faroit/python-wheels
